2026-04-18

在采样时引入随机性：
if random.random() < epsilon:
    # 从有效动作中均匀随机选择
    action = random.choice(valid_actions)
else:
    # 使用策略网络
    action = policy.sample(obs)

2026-04-19

环境问题，每个UAV只能部署一个VNF，修改约束，允许部署多个VNF
结果一般，还是没能学到。

修改环境以及掩码，成功率明显提高，增加训练轮次观察收敛状况

效果不错，已经能够基本与启发式打平，测试多位置--20个位置


2026-04-21
地点数量200测出来效果还行，但是启发式效果也很好，需要大位置数量

2026-04-22
地点数量为400训练了一版ppo_seedNone_20260422_090235

2026-4-25
我将时隙改成10，在不增加UAV的情况下看看，增大SFCs数量DRL和MOM的算法哪个好。

2026-4-26
10时隙的训练效果不好，非常差，感觉可能是环境问题。
环境果然有问题，时隙还是4个，只有SFC变多了，现在重新训练。
重新训练效果不佳，只有40的成功率，尝试和Moploc算法比较:比较结果，不理想，不如MPopLoc效果好。

修改超参数再次训练，这次只使用一个初始状态，观察是否能够过拟合收敛。

2026-5-10
MAPPO训练代码开发完毕，训练效果不错，但收敛非常慢，固定初始环境条件下，需要约1500episode才能收敛



2026-05-11
训练意外终止
mappo_seed42_20260511_160122：MLP，固定环境
mappo_seed42_20260511_145121：atten，100随机环境

2026-05-12
重新训练
MAPPO/output/mappo_seed42_20260513_213420：MLP，固定环境,10wstep
mappo_seed42_20260511_103349：atten，100随机环境

2026-5-14
MAPPO的收敛效果很差尝试以下改进：
1、加入reward_norm 和 层与层之间的norm：几乎无效
2、修改奖励函数，提高稠密奖励，降低惩罚
3、修改为集中式，每次输出所有agent的动作
4、加入余弦退火和评估集，看看能不能有助于收敛和评估：需要看图，看数据没啥效果./layernorm_atten_data100_30uav_warmup

2026-5-18
尝试提高网络层数。MAPPO/output/mappo_seed42_20260518_204735
尝试课程学习。
观察case，看看MAPPO输出方案为什么不合理。

2026-5-21
模型输出是固定的，只会按照一个顺序输出动作。
检查MLP的输出，不同SFC的输出不同

2026-5-23
尝试打乱request的顺序试试